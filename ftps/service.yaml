apiVersion: v1
kind: Service
metadata:
        name: ftps-service
spec:
        type: LoadBalancer
        selector:
                app: ftps
        ports:
                - name : active1-ftp
                  protocol: TCP
                  port: 21
                - name : passive1-ftp
                  protocol: TCP
                  port: 30020
