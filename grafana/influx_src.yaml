apiVersion: 1

datasources:
    - name: influx
      type: influxdb
      access: proxy
      url: http://influxdb:8086
      database: telegraf
      user: telegraf
      password: telegraf 
      basic_auth: false
      jsonData:
          httpMethod: GET
      # is_default: true
